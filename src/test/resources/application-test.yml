spring:
  datasource:
    hikari:
      maximum-pool-size: 2
      minimum-idle: 0
      idle-timeout: 10000
      data-source-properties:
        ssl: false
        sslmode: disable

  jpa:
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    com.lovingapp: DEBUG
